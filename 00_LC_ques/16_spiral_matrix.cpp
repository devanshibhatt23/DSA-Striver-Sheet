#include <bits/stdc++.h>
#define ll long long
using namespace std;

vector<int> solve(vector<vector<int>>& matrix) {
    int m = matrix.size();
    int n = matrix[0].size();

    vector<int> v;

    int k=0;
    while(1) {
        
    }
}

int main() {
    vector<vector<int>> v = {{0,1,2}, {3,4,5}, {1,3,1}};

    solve(v);
}